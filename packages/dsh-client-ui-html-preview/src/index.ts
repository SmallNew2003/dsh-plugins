/**
 * Node half of dsh-client-ui-html-preview. Pure UI plugin: the browser half
 * (exports["./client"]) carries the whole feature; this apply only seats the
 * package in the host Loader so the bundle layer and dsh.client manifest are
 * discovered.
 */
export function apply(): void {}
